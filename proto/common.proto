syntax = "proto3";

package kic.common;

option go_package = "./proto;proto";


message User {
  int64 userID = 1;
  string userName = 2;
}

message File {
  string fileName = 1;
  string fileLocation = 2;
  map<string, string> metadata = 3;
}